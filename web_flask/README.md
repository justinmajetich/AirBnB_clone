
```markdown
# 0x04. AirBnB Clone - Web Framework

![Project Image](project_image.png)

A web framework that mimics the core functionality of the AirBnB website. This project is aims to teach students the fundamentals of web development, server setup, and database integration.

## Table of Contents

- [Introduction](#introduction)
- [Requirements](#requirements)
- [Installation](#installation)
- [More Info](#More Info)


## Introduction

This web framework is a simplified clone of AirBnB, focusing on the core functionality such as:

- User authentication
- Property listing and booking
- Reviews and ratings
- Search functionality
- User dashboards

It is designed to help students learn how to build a full-stack web application, including the back end, front end, and database integration.

## Requirements

# Python Scripts

- Allowed editors: vi, vim, emacs
- All your files will be interpreted/compiled on Ubuntu 20.04 LTS using python3 (version 3.4.3)
- All your files should end with a new line
- The first line of all your files should be exactly #!/usr/bin/python3
- Your code should use the PEP 8 style (version 1.7)
- All your files must be executable
- The length of your files will be tested using wc
- All your modules should have documentation (python3 -c 'print(__import__("my_module").__doc__)')
- All your classes should have documentation (python3 -c 'print(__import__("my_module").MyClass.__doc__)')
- All your functions (inside and outside a class) should have documentation (python3 -c 'print(__import__("my_module").my_function.__doc__)' and python3 -c 'print(__import__("my_module")  MyClass.my_function.__doc__)')
- A documentation is not a simple word, it’s a real sentence explaining what’s the purpose of the module, class or method (the length of it will be verified)

# HTML/CSS Files
- Allowed editors: vi, vim, emacs
- All your files should end with a new line
- Your code should be W3C compliant and validate with W3C-Validator (except for jinja template)
- All your CSS files should be in the styles folder
- All your images should be in the images folder
- You are not allowed to use !important or id (#... in the CSS file)
- All tags must be in uppercase
- Current screenshots have been done on Chrome 56.0.2924.87.
- No cross browsers

## Installation

1. Clone this repository to your local machine:

   ```shell
   git clone https://github.com/azukauteh/AirBnB_clone_v2.git
   ```

2. Navigate to the project directory:

   ```shell
   cd web_flask
   ```

3. Create a virtual environment (optional but recommended):

   ```shell
    python -m venv venv

   ```

## Usage

1. Start the development server:

   ```shell
   python manage.py run
   ```

2. Open your web browser and navigate to `http://localhost:5000` to access the web application.

3. Register a new user or use the provided test user accounts for testing.

4. Explore the functionality of the AirBnB clone web framework, including user authentication, property listing, booking, reviews, and more.

## More Info

- Install Flask

```shell
 pip3 install Flask
```
